name: taskly

services:
  database:
    image: mongo:7
    container_name: taskly-database
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
    name: taskly-mongo-data
